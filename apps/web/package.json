{
  "name": "@feedxml/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "//vercel-build": "Vercel builds with this app as its root, so the workspace libraries it imports must be compiled here first (dependency order matters).",
    "vercel-build": "npm run build --prefix ../../packages/shared && npm run build --prefix ../../packages/domain && next build",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.650.0",
    "@aws-sdk/s3-request-presigner": "^3.650.0",
    "@feedxml/domain": "*",
    "@feedxml/shared": "*",
    "bcryptjs": "^2.4.3",
    "next": "^15.0.0",
    "pg": "^8.13.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^22.7.0",
    "@types/pg": "^8.11.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/three": "^0.185.4",
    "typescript": "^5.6.0"
  }
}
